{
  "name": "domic-app",
  "version": "0.2.0",
  "repository": "https://github.com/ceymard/domic-state",
  "description": "A client-side application state machine for Carbyne.",
  "license": "MIT",
  "typings": "./app.d.ts",
  "main": "./app.js",
  "scripts": {
    "build:js": "tsc",
    "build": "npm run build:js",
    "prepublish": "npm run build",
    "watch": "tscc -w"
  },
  "dependencies": {
    "domic": "*"
  },
  "devDependencies": {
    "dts-bundle": "^0.5.0"
  }
}
